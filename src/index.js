import React from 'react';
import ReactDOM from 'react-dom/client';
import UpdateScreen from './overview/updatingScreen';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
<UpdateScreen/>
  </React.StrictMode>
);
